# docs-pipeline
CI-validated technical documentation — Markdown lint, broken-link checks, and cURL/Python samples verified against live OpenAPI specs via a Prism mock server. Docs treated as code, not static files.
